apiVersion: v1
kind: Service
metadata:
  name: qwen3-tts-ui
  namespace: sietch-sentia
spec:
  selector:
    app: qwen3-tts-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
