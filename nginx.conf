server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://qwen3-tts-api:8888/api/;
        proxy_read_timeout 600s;
        proxy_connect_timeout 10s;
        proxy_send_timeout 600s;
        proxy_buffering off;
        proxy_cache off;
        proxy_set_header Connection '';
        proxy_http_version 1.1;
        chunked_transfer_encoding on;
        add_header X-Accel-Buffering no always;
    }
    location /reference-audio {
        proxy_pass http://qwen3-tts-api:8888;
        proxy_read_timeout 30s;
    }
    location /healthz {
        proxy_pass http://qwen3-tts-api:8888/healthz;
        proxy_read_timeout 5s;
    }
    location /status {
        proxy_pass http://qwen3-tts-api:8888/status;
        proxy_read_timeout 10s;
    }
    location /info {
        proxy_pass http://qwen3-tts-api:8888/info;
        proxy_read_timeout 10s;
    }
    location /reset {
        proxy_pass http://qwen3-tts-api:8888/reset;
        proxy_read_timeout 30s;
    }
}
